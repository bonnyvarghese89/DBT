version: 2

models:
  - name: dim_vendor
    description: Vendor dimension table

    columns:
      - name: vendor_sk
        tests:
          - not_null
          - unique

      - name: vendor_id
        tests:
          - not_null
          - unique

      - name: country
        tests:
          - not_null